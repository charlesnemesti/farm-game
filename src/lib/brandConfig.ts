// NOTE: All code must stay in English, even when requirements arrive in Spanish.

/** Public game title shown in metadata, docs, and UI copy. */
export const GAME_NAME = "Corn Farm";

/** In-game and on-chain currency ticker — always include the $ prefix. */
export const CURRENCY_TICKER = "$CORN";

/** localStorage key prefix (kebab-case). */
export const STORAGE_PREFIX = "cornfarm";
